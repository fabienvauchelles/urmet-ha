"""The domain: the frozen wire vocabulary, the typed errors, and the ports.

The innermost layer. It imports nothing from the project and no framework, so the
use cases can depend on it without dragging a framework in behind them.
"""
